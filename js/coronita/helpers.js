Coronita.helpers = function () {
  function isHorizontal(image) {
    return image.offsetWidth / image.offsetHeight > 1;
  }

  return {
    isHorizontal: isHorizontal
  };
}();
//# sourceMappingURL=helpers.js.map
